# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-freq_hopping
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND freq_hopping_sources
    slot_frame_impl.cc
    bb_pskmod_impl.cc
    hop_interp_impl.cc
    hop_mod_impl.cc
    hop_demod_impl.cc
)

set(freq_hopping_sources "${freq_hopping_sources}" PARENT_SCOPE)
if(NOT freq_hopping_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT freq_hopping_sources)

add_library(gnuradio-freq_hopping SHARED ${freq_hopping_sources})
target_link_libraries(gnuradio-freq_hopping PUBLIC
        gnuradio::gnuradio-runtime
        gnuradio-filter
        liquid::liquid)
target_include_directories(gnuradio-freq_hopping
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-freq_hopping PROPERTIES DEFINE_SYMBOL "gnuradio_freq_hopping_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-freq_hopping PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-freq_hopping)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_freq_hopping_sources
    qa_hop_demod.cc
    qa_bb_pskmod.cc
    qa_slot_frame.cc
    qa_hop_mod.cc
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-freq_hopping gnuradio-blocks)

if(NOT test_freq_hopping_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_freq_hopping_sources)

foreach(qa_file ${test_freq_hopping_sources})
    GR_ADD_CPP_TEST("freq_hopping_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
